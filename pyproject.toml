[project]
name = "gmaps2vcard"
version = "0.1.0"
description = "Convert Google Maps business links to vCard contacts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.5",
    "vobject>=0.9.9",
]
